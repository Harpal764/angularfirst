import { NameformatePipe } from './nameformate.pipe';

describe('NameformatePipe', () => {
  it('create an instance', () => {
    const pipe = new NameformatePipe();
    expect(pipe).toBeTruthy();
  });
});
