import { SearchcustomerPipe } from './searchcustomer.pipe';

describe('SearchcustomerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcustomerPipe();
    expect(pipe).toBeTruthy();
  });
});
